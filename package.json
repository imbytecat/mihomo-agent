{
  "name": "ufi-mihomo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tailwindcss -i src/style.css -o .build/plugin.css --minify && tsdown",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tailwindcss": "^3.4.19",
    "tsdown": "latest",
    "typescript": "^5.9.3"
  }
}
